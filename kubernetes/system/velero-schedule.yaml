apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: uptime-kuma-daily-backup
  namespace: velero
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - uptime-kuma
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
